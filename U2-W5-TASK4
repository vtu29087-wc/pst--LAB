class Solution {
    public int maxSubarraySumCircular(int[] nums) {
        int totalSum = 0;
        int maxSubarraySum = nums[0];
        int currentMax = 0;
        int minSubarraySum = nums[0];
        int currentMin = 0;

        for (int num : nums) {
            totalSum += num;

            // Standard Kadane's algorithm for Maximum Subarray Sum
            currentMax = Math.max(num, currentMax + num);
            maxSubarraySum = Math.max(maxSubarraySum, currentMax);

            // Kadane's algorithm variation for Minimum Subarray Sum
            currentMin = Math.min(num, currentMin + num);
            minSubarraySum = Math.min(minSubarraySum, currentMin);
        }

        // If all numbers are negative, totalSum == minSubarraySum
        if (maxSubarraySum < 0) {
            return maxSubarraySum;
        }

        return Math.max(maxSubarraySum, totalSum - minSubarraySum);
    }
}
